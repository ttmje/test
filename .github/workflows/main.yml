name: test-action
on:
  push:
    branches: [ main ]
  
jobs:
  my_testing:
    runs-on: ubuntu-latest
    steps:
      - name: Run hello message
        run: echo "Hello, world! from testing job"
          
  my_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Run hello message
        run: echo "Hello, world! from deploy job"
    
